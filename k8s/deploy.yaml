apiVersion: apps/v1
kind: Deployment
metadata:
  name: hikkasay-bot
  namespace: hikkasay
spec:
  replicas: 1
  strategy:
    type: Recreate # Сначала убить старый, потом запустить новый
  selector:
    matchLabels:
      app: hikkasay
  template:
    metadata:
      labels:
        app: hikkasay
    spec:
      imagePullSecrets:
        - name: ghcr-auth # Тот самый секрет, который мы создали выше
      containers:
        - name: bot
          image: ghcr.io/succeio/deno-hikkasay:latest
          imagePullPolicy: Always
          envFrom:
            - secretRef:
                name: bot-secrets
            - configMapRef:
                name: hikkasay-config
          volumeMounts:
            - name: storage
              mountPath: /app/data
      volumes:
        - name: storage
          persistentVolumeClaim:
            claimName: hikkasay-data-pvc
